import React from "react";
import { View } from "react-native";

// A minimalistic dividing line for styling the pages
const DividingLine = () => {
  return (
    <View
      style={{
        borderBottomColor: "black",
        borderBottomWidth: 1,
      }}
    />
  );
};

export default DividingLine;
